# Generated by `pnpm --dir site snapshot`. Commit specific meeting snapshots
# individually with `git add -f site/snapshots/<name>.pdf` when you want to
# pin a talk — don't check the whole directory in.
*.pdf
!.gitignore
